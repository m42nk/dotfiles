- defaults:
    link:
      relink: true

- clean: ['~', '~/.config']

# - link:
    # ~/.tmux.conf: tmux.conf
    # ~/.vim: vim
    # ~/.vimrc: vimrc

- create:
    - ~/.config

- shell:
  - [git submodule update --init --recursive, Installing submodules]
