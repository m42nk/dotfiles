- defaults:
    link:
      relink: true

- clean: ['~', '~/.config']

- link:
     ~/.config/nvim: nvim
     ~/.config/tmux: tmux

- create:
    - ~/.config

- shell:
  - [git submodule update --init --recursive, Installing submodules]
