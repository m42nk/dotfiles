session: BLOG

root: ~/Codes/Blog

before_start:
  # - docker-compose up -d

stop:
  # - docker-compose down

windows:
  - name: Main
    root: .
    commands:
      - clear

  - name: Server
    root: .
    commands:
      - clear
      - yarn dev
    # panes:
    #   - type: tiled
    #     root: .
    #     commands:
    #       - clear
    #   - type: tiled
    #     root: ../Web
    #     commands:
    #       - clear

